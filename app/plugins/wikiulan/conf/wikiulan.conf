# Uncomment the following to override menu entry labels in the "History" menu
#
# menuItemNames = {
#	ca_objects = Items
# }
templates = {
    first = {
        table = "ca_objects",
        label = "Obras (lista de artistas)"
    }, 
    second = {
        table = "ca_objects",
        label = "Obras (lista de exhibições)"
    }, 
}
